{
  "name": "cf-email-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker 用于接收邮件并通过 API 转发，同时将附件上传到 R2",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "email",
    "r2",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240404.0",
    "html-to-text": "^9.0.5",
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.10.0"
  }
}
